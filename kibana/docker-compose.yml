version: '3'

services:
  kibana:
    image: docker.elastic.co/kibana/kibana:7.17.9
    container_name: kibana
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://10.0.1.218:9200
      - NODE_OPTIONS=--max-old-space-size=256
    restart: always